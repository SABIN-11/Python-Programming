# def getSecondLargest(self, arr):
    # size = len(arr)
    # max = -1
    # s_max = -1
    # for i in range(size):
        # if arr[i] > max:
            # s_max = max
            # max = arr[i]
        # if arr[i] > s_max and arr[i] < max:
            # s_max = arr[i]
            # 
    # return s_max