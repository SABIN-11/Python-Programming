# def subarraySum(self, arr, target):
    
    # size = len(arr)
    # 
    # for i in range(size):
        # sum = arr[i]
        # if sum == target:
            # return [i + 1, i + 1]
        # for j in range(i + 1, size):
            # sum += arr[j]
            # if sum > target:
                # break
            # if sum == target:
                # return [i + 1, j + 1]
                # 
    # return [-1]